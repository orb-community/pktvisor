## Scenario: Run pktvisor using docker command 
## Steps: 
 - Run docker using `docker run --net=host -d ns1labs/pktvisor pktvisord <net>`
 

## Expected Result:
 - Pktvisor container must be running
 - Endpoints from pktvisor API must be accessible
 
